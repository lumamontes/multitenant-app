# Auto-generated workflow for Tenant One App
# Generated at: 2025-06-01T08:45:49.700Z
# DO NOT EDIT MANUALLY - This file is auto-generated by scripts/generateConfigs.ts

name: Build TenantOne

on:
  workflow_call:
    inputs:
      platform:
        description: 'Platform to build'
        required: true
        default: 'all'
        type: string
      profile:
        description: 'Build profile'
        required: true
        default: 'production'
        type: string
      skip-build:
        description: 'Skip build and only do OTA update'
        required: false
        default: false
        type: boolean
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to build'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - ios
          - android
      profile:
        description: 'Build profile'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - preview
          - development
      skip-build:
        description: 'Skip build and only do OTA update'
        required: false
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TENANT_ID: tenant1
  TENANT_NAME: "Tenant One App"
  BUNDLE_IDENTIFIER: "com.yourcompany.tenantone"
  PACKAGE_NAME: "com.yourcompany.tenantone"

jobs:
  # Check fingerprint to decide between OTA update or new build
  fingerprint:
    name: 📷 Check Fingerprint
    runs-on: ubuntu-latest
    outputs:
      needs-build: ${{ steps.fingerprint.outputs.includes-changes }}
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: 📦 Install dependencies
        run: npm ci

      - name: 📱 Generate tenant config
        run: npm run config:generate
        env:
          EXPO_PUBLIC_TENANT: tenant1

      - name: 📷 Check fingerprint
        id: fingerprint
        run: |
          # Check if native code changed using expo fingerprint
          if npx @expo/fingerprint@latest --platform all --json > current-fingerprint.json; then
            # Compare with cached fingerprint
            if [ -f "fingerprint-cache-tenant1.json" ]; then
              if ! cmp -s current-fingerprint.json fingerprint-cache-tenant1.json; then
                echo "includes-changes=true" >> $GITHUB_OUTPUT
                echo "🔄 Native changes detected for tenant1"
              else
                echo "includes-changes=false" >> $GITHUB_OUTPUT
                echo "✅ No native changes for tenant1"
              fi
            else
              echo "includes-changes=true" >> $GITHUB_OUTPUT
              echo "🆕 First build for tenant1"
            fi
            cp current-fingerprint.json fingerprint-cache-tenant1.json
          else
            echo "includes-changes=true" >> $GITHUB_OUTPUT
          fi

      - name: 💾 Cache fingerprint
        uses: actions/cache@v4
        with:
          path: fingerprint-cache-tenant1.json
          key: fingerprint-tenant1-${{ github.sha }}
          restore-keys: |
            fingerprint-tenant1-

  # OTA Update (if no native changes)
  ota-update:
    name: 📦 OTA Update
    runs-on: ubuntu-latest
    needs: [fingerprint]
    if: ${{ needs.fingerprint.outputs.needs-build == 'false' && !inputs.skip-build }}
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔤 Compile translations
        run: npm run intl:build || echo "No translations to compile"

      - name: ✏️ Write environment variables
        run: |
          echo "${{ secrets.TENANT1_ENV_TOKEN }}" > .env
          echo "EXPO_PUBLIC_BUNDLE_IDENTIFIER=$(git rev-parse --short HEAD)" >> .env
          echo "EXPO_PUBLIC_BUNDLE_DATE=$(date -u +"%y%m%d%H")" >> .env

      - name: 📱 Generate tenant config
        run: npm run config:generate
        env:
          EXPO_PUBLIC_TENANT: tenant1

      - name: 🚀 Deploy OTA Update
        run: |
          eas update \
            --channel ${{ inputs.profile || 'production' }} \
            --message "OTA update for TenantOne - ${{ github.event.head_commit.message || 'Manual deployment' }}" \
            --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_PUBLIC_TENANT: tenant1

  # iOS Build
  build-ios:
    name: 🍎 Build iOS
    runs-on: macos-15
    needs: [fingerprint]
    if: ${{ always() && (needs.fingerprint.outputs.needs-build == 'true' || inputs.skip-build == false) && contains(fromJson('["all", "ios"]'), inputs.platform) }}
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: 🍎 Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.4'

      - name: ♻️ Restore CocoaPods cache
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔤 Compile translations
        run: npm run intl:build || echo "No translations to compile"

      - name: ✏️ Write environment variables
        run: |
          echo "${{ secrets.TENANT1_ENV_TOKEN }}" > .env
          echo "EXPO_PUBLIC_BUNDLE_IDENTIFIER=$(git rev-parse --short HEAD)" >> .env
          echo "EXPO_PUBLIC_BUNDLE_DATE=$(date -u +"%y%m%d%H")" >> .env
          echo "${{ secrets.TENANT1_GOOGLE_SERVICES_TOKEN }}" > google-services.json

      - name: 📱 Generate tenant config
        run: npm run config:generate
        env:
          EXPO_PUBLIC_TENANT: tenant1

      - name: 🏗️ EAS Build iOS
        run: |
          PROFILE="tenant1-${{ inputs.profile || 'production' }}"
          npm run use-build-number-with-bump eas build \
            --platform ios \
            --profile $PROFILE \
            --local \
            --output build-tenant1-ios.ipa \
            --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_PUBLIC_TENANT: tenant1

      - name: 📤 Upload iOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: tenant1-ios-${{ inputs.profile || 'production' }}
          path: build-tenant1-ios.ipa
          retention-days: 30

      - name: 🚀 Submit to App Store
        if: ${{ contains(fromJson('["production"]'), inputs.profile) }}
        run: eas submit --platform ios --non-interactive --path build-tenant1-ios.ipa
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

  # Android Build
  build-android:
    name: 🤖 Build Android
    runs-on: ubuntu-latest
    needs: [fingerprint]
    if: ${{ always() && (needs.fingerprint.outputs.needs-build == 'true' || inputs.skip-build == false) && contains(fromJson('["all", "android"]'), inputs.platform) }}
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔤 Compile translations
        run: npm run intl:build || echo "No translations to compile"

      - name: ✏️ Write environment variables
        run: |
          echo "${{ secrets.TENANT1_ENV_TOKEN }}" > .env
          echo "EXPO_PUBLIC_BUNDLE_IDENTIFIER=$(git rev-parse --short HEAD)" >> .env
          echo "EXPO_PUBLIC_BUNDLE_DATE=$(date -u +"%y%m%d%H")" >> .env
          echo "${{ secrets.TENANT1_GOOGLE_SERVICES_TOKEN }}" > google-services.json

      - name: 📱 Generate tenant config
        run: npm run config:generate
        env:
          EXPO_PUBLIC_TENANT: tenant1

      - name: 🏗️ EAS Build Android
        run: |
          PROFILE="tenant1-${{ inputs.profile || 'production' }}-android"
          npm run use-build-number-with-bump eas build \
            --platform android \
            --profile $PROFILE \
            --local \
            --output build-tenant1-android.apk \
            --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_PUBLIC_TENANT: tenant1

      - name: 📤 Upload Android artifact
        uses: actions/upload-artifact@v4
        with:
          name: tenant1-android-${{ inputs.profile || 'production' }}
          path: build-tenant1-android.apk
          retention-days: 30

      - name: 🚀 Submit to Play Store
        if: ${{ contains(fromJson('["production"]'), inputs.profile) }}
        run: eas submit --platform android --non-interactive --path build-tenant1-android.apk
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

  # Notify completion
  notify:
    name: 📢 Notify
    runs-on: ubuntu-latest
    needs: [build-ios, build-android, ota-update]
    if: always()
    steps:
      - name: 📱 Send Slack notification
        if: ${{ secrets.TENANT1_SLACK_WEBHOOK_URL }}
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: |
            TenantOne build completed!
            iOS: ${{ needs.build-ios.result }}
            Android: ${{ needs.build-android.result }}
            OTA: ${{ needs.ota-update.result }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.TENANT1_SLACK_WEBHOOK_URL }}
